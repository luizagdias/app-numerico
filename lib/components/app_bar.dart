import 'package:flutter/material.dart';

AppBar appBar() {
  return AppBar(
    title: const Text(
      '# App Numérico',
      style: TextStyle(
        fontSize: 20,
      ),
    ),
    backgroundColor: const Color(0xFF558B2F),
  );
}
